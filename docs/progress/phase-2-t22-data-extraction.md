# Phase 2: Region Dictionary Split

- [x] Extract `REGIONS` into `frontend/src/shared/data/regions/catalog.js`.
- [x] Add `findRegionByValue(...)`.
- [x] Add `getCitiesByProvince(...)`.

## Notes

- The region dictionary remains a static local data source.
- New helpers reduce future shared-data duplication without requiring component edits in this task.
