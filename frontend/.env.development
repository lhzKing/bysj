# 该文件被仓库追踪，**不要写入任何敏感真值**。
# 真实凭据（如高德 Web Key）请放到本地未追踪文件 frontend/.env.development.local
# 该文件的占位符仅起"提示开发者需要配置哪些变量"的作用；
# 当 .env.development 与 .env.development.local 同时存在时，Vite 会以 .local 为准（不会回退到这里）。
#
# 推荐流程：
#   1. cp frontend/.env.example frontend/.env.development.local
#   2. 在 .env.development.local 内填入你的真实 Key
#   3. 不要修改本文件（除非需要新增公开占位符）

VITE_AMAP_KEY=<replace-with-amap-web-js-api-key>
VITE_API_PROXY_TARGET=http://localhost:8080
