# 项目审查整改执行任务表

> 适用项目：当前工作区 `d:\bysj`  
> 来源文档：`项目审查.md`（整合版严格代码审查记录）  
> 生成日期：2026-05-02  
> 文件用途：将审查结论拆解为可执行整改任务，供后续会话按任务编号持续推进。  
> 重要说明：本文件是新的任务表文件，不覆盖、不修改原 `项目整改执行任务表.md`。

---

## 一、使用说明

后续新对话可以直接发送：

> **请按照 `项目审查整改执行任务表.md` 继续推进整改。每次只处理一个任务或一组强相关子任务；完成后更新任务状态、验收结果和更新记录。**

执行要求：

1. 每次开始前先读取本文件。
2. 优先查看“当前进行中任务”和“最近一次更新摘要”。
3. 若无进行中任务，按 P0 → P1 → P2 顺序选择第一个 `TODO` 任务。
4. 每次只推进一个任务，或一组强相关任务。
5. 每完成一项，必须更新：
   - 当前进行中任务
   - 最近一次更新摘要
   - 当前任务状态总览
   - 对应任务明细中的验收结果
   - 更新记录
6. 如果实施中发现任务需要拆分，在本文件内补充子任务，不只在对话里说明。
7. 如果任务因需求不明确、环境缺失、测试无法运行等原因无法继续，状态改为 `BLOCKED` 并写明阻塞原因。

---

## 二、状态说明

| 状态 | 含义 |
|---|---|
| `TODO` | 未开始 |
| `DOING` | 正在处理 |
| `BLOCKED` | 被阻塞，需要用户确认或外部条件 |
| `DONE` | 已完成并通过验收 |
| `SKIPPED` | 用户确认跳过或不再适用 |

优先级说明：

| 优先级 | 含义 |
|---|---|
| `P0` | 立即处理；安全、数据破坏、强一致性风险 |
| `P1` | 核心业务、契约、权限、测试可信度问题 |
| `P2` | 可维护性、文档债、工程卫生、长期治理 |

---

## 三、当前进行中任务

> 没有进行中的任务时填写“无”。开始执行新任务时，先更新这里。

- 当前任务编号：无
- 当前任务名称：无
- 当前状态：无
- 开始时间：无
- 当前执行人/会话说明：R-P2-08 已完成，审查整改任务表当前全部任务关闭
- 当前目标：无
- 当前阻塞点：无
- 下一步动作：无；如需继续可进入新一轮复核或补充集成/端到端环境测试

---

## 四、最近一次更新摘要

- 最近更新日期：2026-05-03
- 最近完成任务：R-P2-08 补充启动路径与异常场景集成测试
- 最近修改文件：`backend/src/test/java/com/example/trace/config/WebMvcSecurityPathContractTest.java`、`docs/analysis/2026-05-03-rp208-startup-and-exception-coverage.md`、`docs/progress/MASTER.md`、`项目审查整改执行任务表.md`
- 最近验证结果：`python tools/check_text_encoding.py` 通过（444 files）；`python tools/check_text_encoding.py --include-runtime` 通过（669 files）；`mvn test "-Dtest=WebMvcSecurityPathContractTest"` 通过；R-P2-08 相关后端定向测试通过；`mvn test -q` 通过；`npm run test:run` 通过（24 files，67 tests）。
- 当前整体进度：23 项 `DONE`，0 项 `DOING`，0 项 `BLOCKED`，0 项 `SKIPPED`，0 项 `TODO`
- 下一推荐任务：无（本任务表已全部完成）

---

## 五、任务状态总览

| 任务编号 | 任务名称 | 优先级 | 状态 | 主要范围 | 备注 |
|---|---|---:|---|---|---|
| R-P0-01 | 安全配置 profile 化与默认密钥 fail-fast | P0 | DONE | backend config | 已完成 dev/test/prod 拆分、prod fail-fast、日志隔离与测试 |
| R-P0-02 | RSA 私钥移除、轮换与签名密钥外置 | P0 | DONE | backend keys/signature | 已配置 D:/trace-runtime/keys 固定外部密钥并补 keyId/keyVersion |
| R-P0-03 | 敏感日志脱敏与 SQL 日志按环境隔离 | P0 | DONE | backend logging | 已移除完整 Authorization Header 日志，prod/default 不启用 SQL stdout |
| R-P0-04 | TokenStore Redis 异常语义改为 fail-closed | P0 | DONE | backend auth/token | Redis 黑名单异常已 fail-closed，写入失败会返回 503 |
| R-P0-05 | 地图 InfoWindow XSS 修复与节点字段校验 | P0 | DONE | frontend map + backend DTO | InfoWindow 改为 DOM/textContent，后端节点字段已校验 |
| R-P0-06 | 高危 Admin 数据清理接口隔离与二次确认 | P0 | DONE | backend admin | 默认仅 dev/test 启用；清理接口需专用权限 + `confirm` |
| R-P1-01 | 自助注册链路与后台用户创建解耦 | P1 | DONE | frontend login + backend auth | 登录页改走 `/auth/register`，后台 `/users` 保持管理入口 |
| R-P1-02 | 提取 AuthService，降低 AuthController 厚度 | P1 | DONE | backend auth | 认证业务已下沉到 AuthService，Controller 仅保留 HTTP 入口 |
| R-P1-03 | 扫码权限模型收敛 | P1 | DONE | backend permission + frontend route | `trace:scan` 收敛为超级扫码权限，细粒度权限仅允许对应动作 |
| R-P1-04 | 事件 remark 契约收敛 | P1 | DONE | trace DTO/entity/schema/frontend/docs | remark 保留为正式审计字段，已落库并纳入非空备注 Hash/签名 |
| R-P1-05 | eventTime 严格校验，禁止解析失败 fallback now | P1 | DONE | backend DateTimeUtil + frontend format | 非空 eventTime 已严格要求 ISO-8601，本地时间非法格式返回 400 |
| R-P1-06 | 配件删除引用完整性保护 | P1 | DONE | backend part + SQL | 删除前检查溯源快照/日志引用，已引用配件返回 409 |
| R-P1-07 | 赋码 quantity 与示例数据 count 增加上限 | P1 | DONE | backend trace/admin | `quantity<=500`，示例数据 `count` 硬上限 500 且配置只能下调 |
| R-P1-08 | 前端测试恢复全绿并复核循环依赖 | P1 | DONE | frontend tests/request/router/store | Vitest 全绿；401 跳转改为注入式 handler，解除 request→router 循环 |
| R-P1-09 | 关键文档与接口实现同步 | P1 | DONE | README/api-doc/postman/backend README | 已同步注册、当前用户、refresh、密钥/profile、Redis 与前端 API 示例 |
| R-P2-01 | CORS 处理机制收敛 | P2 | DONE | backend CORS | 保留高优先级 CorsFilter 为唯一 CORS 写入点，WebMvcConfig 仅保留拦截器 |
| R-P2-02 | 角色层级策略集中化 | P2 | DONE | backend user/role | 已提取 RolePolicy 统一角色优先级、系统角色与授权策略 |
| R-P2-03 | 用户 status 更新校验统一 | P2 | DONE | backend user | PUT 更新与状态切换均限制 status 为 0/1 |
| R-P2-04 | localStorage Token 风险评估与 CSP/短 Token 方案 | P2 | DONE | frontend auth/security | 已形成决策记录并落地短 Token、CSP 与存储适配层 |
| R-P2-05 | 工作区运行产物与大文件清理策略 | P2 | DONE | repo hygiene | 已归档运行产物并补充忽略规则与清单 |
| R-P2-06 | 全项目文本编码与乱码治理 | P2 | DONE | project text encoding | 已清理源码/文档/配置/Postman/文章文本及 runtime_state 文本产物，并新增防回归扫描 |
| R-P2-07 | 重写 frontend README | P2 | DONE | frontend docs | 已替换 Vite 模板说明，补齐真实启动、环境变量、HTTPS、代理、测试和构建指引 |
| R-P2-08 | 补充启动路径与异常场景集成测试 | P2 | DONE | backend/frontend tests | 已新增 MVC 启动路径契约测试并汇总异常场景覆盖 |

---

## 六、推荐执行顺序

### 阶段 1：P0 安全与数据破坏风险封堵

1. R-P0-01 安全配置 profile 化与默认密钥 fail-fast
2. R-P0-02 RSA 私钥移除、轮换与签名密钥外置
3. R-P0-03 敏感日志脱敏与 SQL 日志按环境隔离
4. R-P0-04 TokenStore Redis 异常语义改为 fail-closed
5. R-P0-05 地图 InfoWindow XSS 修复与节点字段校验
6. R-P0-06 高危 Admin 数据清理接口隔离与二次确认

### 阶段 2：P1 核心业务契约与权限收敛

7. R-P1-01 自助注册链路与后台用户创建解耦
8. R-P1-02 提取 AuthService，降低 AuthController 厚度
9. R-P1-03 扫码权限模型收敛
10. R-P1-04 事件 remark 契约收敛
11. R-P1-05 eventTime 严格校验
12. R-P1-06 配件删除引用完整性保护
13. R-P1-07 批量数量上限
14. R-P1-08 前端测试恢复全绿并复核循环依赖
15. R-P1-09 关键文档与接口实现同步

### 阶段 3：P2 工程治理与长期维护

16. R-P2-01 CORS 处理机制收敛
17. R-P2-02 角色层级策略集中化
18. R-P2-03 用户 status 更新校验统一
19. R-P2-04 localStorage Token 风险评估与 CSP/短 Token 方案
20. R-P2-05 工作区运行产物与大文件清理策略
21. R-P2-06 全项目文本编码与乱码治理
22. R-P2-07 重写 frontend README
23. R-P2-08 补充启动路径与异常场景集成测试

---

## 七、任务明细

### R-P0-01 安全配置 profile 化与默认密钥 fail-fast

- 优先级：P0
- 状态：DONE
- 来源审查项：默认 JWT 密钥、默认数据库凭据、DEBUG 安全日志、SQL stdout 日志混在默认配置中
- 目标：建立 dev/test/prod 配置边界，长期运行环境禁止使用默认密钥和开发日志配置
- 重点范围：
  - `backend/src/main/resources/application.yml`
  - 可新增 `application-dev.yml` / `application-test.yml` / `application-prod.yml`
  - `backend/.env.example`
  - `backend/src/main/java/com/example/trace/security/JwtUtil.java`
- 实施步骤：
  1. 拆分开发、测试、生产 profile。
  2. 生产 profile 下禁止默认 `TRACE_JWT_SECRET`，未配置或使用默认值时启动失败。
  3. 生产 profile 下禁止默认 `root/root` 数据库凭据。
  4. SQL stdout 日志只允许 dev/test 开启。
  5. 安全包日志生产默认不低于 INFO/WARN。
  6. `.env.example` 中所有密钥改为占位符。
- 验收标准：
  - dev/test 能正常启动和测试。
  - prod 未显式配置 JWT secret 时启动失败。
  - prod 不再默认输出 MyBatis stdout SQL。
  - `.env.example` 不含真实可直接使用的 JWT secret。
- 建议验证：
  - `mvn test -q`
  - 增加配置绑定或启动 fail-fast 单元测试
- 验收结果：DONE（2026-05-02）
  - 已将公共配置与 `application-dev.yml` / `application-test.yml` / `application-prod.yml` 分层；默认 profile 为 dev。
  - `application-prod.yml` 不再提供 JWT secret、数据库账号密码、MyBatis stdout SQL 日志默认值；安全包日志生产默认 `WARN`。
  - 新增 `ProdProfileConfigGuard` 并通过 `spring.factories` 在启动早期执行：prod profile 下缺少 `TRACE_JWT_SECRET`、使用旧默认/dev/test JWT secret、JWT secret 少于 32 bytes、缺少生产数据库配置或使用 `root/root` 时均 fail-fast。
  - `JwtUtil` 不再内置默认 JWT secret，也不再自动补齐短密钥。
  - `backend/.env.example` 已改为占位符模板，不包含可直接使用的 JWT secret 或数据库密码。
  - 验证：`mvn test -Dtest=ProdProfileConfigGuardTest,AuthControllerTest,LoginInterceptorTest,CorsOriginMatcherTest` 通过（13 tests）；`mvn test -q` 通过。
- 是否涉及前端 UI 文件：否

### R-P0-02 RSA 私钥移除、轮换与签名密钥外置

- 优先级：P0
- 状态：DONE
- 来源审查项：`backend/keys/private_key.pem` 当前存在且默认配置加载该路径
- 目标：移除工作区私钥，签名密钥改为运行环境外置注入
- 重点范围：
  - `backend/keys/private_key.pem`
  - `backend/keys/public_key.pem`
  - `backend/src/main/resources/application.yml`
  - `backend/src/main/java/com/example/trace/util/SignatureUtil.java`
  - `README.md` / `backend/README.md`
- 实施步骤：
  1. 确认密钥是否仅为演示用途；若不确定，按已泄露处理。
  2. 从工作区移除私钥文件。
  3. 轮换 RSA 密钥对。
  4. 配置改为环境变量或部署挂载路径注入。
  5. 修正文档，说明自动生成是否落盘、生产密钥如何配置。
- 验收标准：
  - 工作区不再保留真实私钥。
  - 默认配置不再硬编码 `d:/bysj/backend/keys/private_key.pem` 作为长期运行路径。
  - 签名和验链测试仍通过。
- 建议验证：
  - `mvn test -q`
  - 验证 `/api/traces/public-key` 与验链接口行为正常
- 验收结果：DONE（2026-05-02，补强后重新验收）
  - 已保留“工作区不存放私钥”的边界：`backend` 目录无 `*.pem`，外部备份密钥位于 `D:/trace-runtime/keys/private_key.pem` / `public_key.pem`。
  - `application-dev.yml` / `application-test.yml` 已默认指向 `D:/trace-runtime/keys`，`TRACE_SIGNATURE_AUTO_GENERATE` 默认 `false`，本地重启不会再生成临时密钥导致历史签名失效。
  - `application-prod.yml` 继续要求外部注入 key path，并新增 `TRACE_SIGNATURE_KEY_ID` / `TRACE_SIGNATURE_KEY_VERSION`；生产 fail-fast 会拦截缺失/占位符/非法 key metadata。
  - 新日志会落库 `signature_key_id` / `signature_key_version`；`init_schema.sql` 与 `migrate_v3_signature_key_metadata.sql` 已补充字段、索引和旧数据回填方案（默认 `default` / `1`）。
  - `TraceLogFactory` / `TraceDemoDataServiceImpl` 写入签名时同步保存 key metadata；`TraceChainVerifyService` 验签前按日志声明的 keyId/keyVersion 判断当前公钥是否匹配，不再无条件使用当前公钥验证所有历史签名。
  - `/api/traces/public-key` 返回 `publicKey`、`keyId`、`keyVersion`、算法信息；`ChainVerifyResponse` 返回 anchor key metadata，错误详情包含 log key metadata。
  - 验证：`mvn test -Dtest=SignatureUtilTest,ProdProfileConfigGuardTest,TraceChainVerifyServiceTest,TraceServiceImplTest,TraceScanTransactionServiceTest,TraceDemoDataServiceImplTest` 通过（24 tests）；`mvn test -q` 通过。
  - 限制说明：当前工作区不是 Git 仓库，无法本地验证历史提交或外部分发包；当前按用户提供的备份密钥作为 `default` / `1` 固定密钥继续使用。
- 是否涉及前端 UI 文件：否
- 待验证：当前私钥是否已进入 Git 历史或外部分发包（本工作区无 `.git`，本轮无法验证历史；已按泄露处理）

### R-P0-03 敏感日志脱敏与 SQL 日志按环境隔离

- 优先级：P0
- 状态：DONE
- 来源审查项：`LoginInterceptor` DEBUG 记录完整 Authorization Header；MyBatis 默认 stdout SQL
- 目标：消除 Token、密码、敏感 Header、SQL 参数的日志泄露风险
- 重点范围：
  - `backend/src/main/java/com/example/trace/security/LoginInterceptor.java`
  - `backend/src/main/resources/application.yml`
  - logging/profile 配置
- 实施步骤：
  1. 删除 `Authorization header` 原文日志。
  2. 如需诊断，仅打印是否存在 Header、JTI 或 hash 摘要。
  3. 检索所有 `log.debug`、`log.info` 中是否包含 token/password/secret/Authorization。
  4. MyBatis SQL stdout 日志按 profile 隔离。
- 验收标准：
  - 日志中不出现完整 Bearer Token。
  - 生产默认不启用 SQL stdout。
  - 后端测试通过。
- 建议验证：
  - `rg -n "Authorization header|password|token|secret" backend/src/main/java`
  - `mvn test -q`
- 验收结果：DONE（2026-05-02）
  - `LoginInterceptor` 已删除完整 `Authorization` Header 原文日志，仅记录 Header 是否存在及是否为 Bearer scheme；新增回归测试断言日志不包含 `Bearer raw-secret-token` / `raw-secret-token`。
  - `JwtUtil` 验证/解析异常日志不再输出 parser exception message，仅输出固定原因或异常类型；`TokenStore` Redis/JWT 异常日志同样不再输出 `e.getMessage()`。
  - `AuthController` / `UserServiceImpl` 登录失败、改密/重置密码相关日志已改为 credential/session 语义，不输出密码原文、Token 原文或 Header 原文。
  - SQL 日志继续按 profile 隔离：`StdOutImpl` 仅位于 `application-dev.yml` / `application-test.yml`，`application.yml` / `application-prod.yml` 无 MyBatis stdout SQL。
  - 验证：`mvn test -Dtest=LoginInterceptorTest,AuthControllerTest,ProdProfileConfigGuardTest` 通过（16 tests）；`mvn test -q` 通过；`rg -n "Authorization header: \\[" backend/src/main/java` 无命中；`rg -n "StdOutImpl" backend/src/main/resources/application-prod.yml backend/src/main/resources/application.yml` 无命中；`backend/src/main/java/com/example/trace/security` 无 `e.getMessage()` 日志。
- 是否涉及前端 UI 文件：否

### R-P0-04 TokenStore Redis 异常语义改为 fail-closed

- 优先级：P0
- 状态：DONE
- 来源审查项：`TokenStore.isBlacklisted()` 捕获 Redis 异常后返回 false
- 目标：修复 Token 黑名单在 Redis 异常时失败即放行的问题
- 重点范围：
  - `backend/src/main/java/com/example/trace/security/TokenStore.java`
  - `backend/src/main/java/com/example/trace/security/LoginInterceptor.java`
  - `backend/src/main/java/com/example/trace/controller/AuthController.java`
  - `backend/src/test/java/com/example/trace/security/...`
- 实施步骤：
  1. 明确 Redis 黑名单是强安全依赖还是可降级能力。
  2. 生产环境下 Redis 异常时认证链路应拒绝或返回认证系统不可用。
  3. `addToBlacklist` 失败时向登出/刷新/改密调用方暴露失败。
  4. 为 Redis 异常场景补测试。
- 验收标准：
  - Redis 异常不会导致已登出 Token 默认放行。
  - 登出、刷新、改密的 Token 失效语义有明确失败处理。
  - 新增测试覆盖 Redis 异常。
- 建议验证：
  - `mvn test -q`
- 验收结果：DONE（2026-05-02）
  - 已将 Redis 黑名单作为认证安全依赖处理：新增 `TokenStoreException`，`TokenStore.isBlacklisted()` 遇到 Redis 查询异常时不再返回 `false`，而是抛出异常并由认证链路 fail-closed。
  - `LoginInterceptor` 调整为先校验 JWT，再检查黑名单；当黑名单存储不可用时直接拒绝请求并返回 `503` / `BizCode.SERVER_ERROR`，不会继续解析用户或放行。
  - `AuthController.refreshToken()` 调整为先校验旧 Token，再检查黑名单；黑名单读/写异常通过全局异常处理器返回 `503`。
  - `TokenStore.addToBlacklist()` 遇到 JTI 缺失或 Redis 写入失败时不再静默吞掉，登出、刷新、改密调用方会收到失败；`GlobalExceptionHandler` 对 `TokenStoreException` 统一返回“认证状态存储暂不可用，请稍后重试”。
  - 新增/补充测试覆盖：Redis 查询异常 fail-closed、Redis 写入异常抛出、JTI 缺失写入失败、登录拦截器 503 拒绝、登出/刷新/改密黑名单失败向上暴露、MockMvc 503 响应。
  - 验证：`mvn test -Dtest=TokenStoreTest,LoginInterceptorTest,AuthControllerTest,ControllerQueryBindingTest` 通过（21 tests）；`mvn test -q` 通过。
- 是否涉及前端 UI 文件：否

### R-P0-05 地图 InfoWindow XSS 修复与节点字段校验

- 优先级：P0
- 状态：DONE
- 来源审查项：`TraceRouteMap.vue` 使用业务字段拼接 InfoWindow HTML
- 目标：防止溯源节点名称触发存储型 XSS
- 重点范围：
  - `frontend/src/features/trace/components/TraceRouteMap.vue`
  - `backend/src/main/java/com/example/trace/dto/ScanTraceRequest.java`
  - `backend/src/main/java/com/example/trace/service/impl/support/TraceScanTransactionService.java`
  - 前端/后端相关测试
- 实施步骤：
  1. 前端 InfoWindow 内容改为 DOM API + `textContent`，或统一 HTML escape。
  2. 所有进入 InfoWindow 的字段都要安全处理。
  3. 后端对 `fromNode` / `toNode` / `province` / `city` 增加长度和字符集校验。
  4. 增加恶意节点名回归测试。
- 验收标准：
  - 节点名为 `<img onerror=...>` 时不会作为 HTML 执行。
  - 正常地图渲染不受影响。
  - 后端拒绝超长或非法节点名。
- 建议验证：
  - `npm run test:run` 中相关测试通过
  - `mvn test -q`
- 验收结果：DONE（2026-05-02）
  - `TraceRouteMap.vue` 的 InfoWindow 内容已不再通过模板字符串拼接业务字段，改为调用 `createTraceRouteInfoWindowContent(...)` 使用 DOM API 和 `textContent` 渲染节点名、节点类型和时间。
  - 新增前端回归测试：节点名为 `<img src=x onerror=...>` 时，InfoWindow DOM 中不会生成 `img` 元素，恶意内容仅作为文本出现。
  - `ScanTraceRequest` 已对 `fromNode` / `toNode` 增加 64 字符上限，对 `province` / `city` 增加 32 字符上限，并统一限制为中文、字母、数字、空格、短横线、下划线、括号、间隔号和点号等安全字符。
  - `TraceScanTransactionService` 在事务入口增加防御式 normalize + 校验：空白字段转为 `null`，非法/超长字段抛 `BizCode.PARAM_ERROR`，并在持久化前中止。
  - 验证：`npm run test -- --run src/features/trace/components/__tests__/TraceRouteMap.info-window.test.js` 通过（2 tests）；`npm run build` 通过（仅既有 chunk size warning）；`mvn test -Dtest=ScanTraceRequestValidationTest,TraceScanTransactionServiceTest` 通过（6 tests）；`mvn test -q` 通过。
- 是否涉及前端 UI 文件：是，但属于安全修复，不是视觉改造

### R-P0-06 高危 Admin 数据清理接口隔离与二次确认

- 优先级：P0
- 状态：DONE
- 来源审查项：`/api/admin/clear-trace-data` 可清空全部溯源日志和快照
- 目标：避免演示/维护接口在长期运行环境造成数据破坏
- 重点范围：
  - `backend/src/main/java/com/example/trace/controller/AdminController.java`
  - `backend/src/main/java/com/example/trace/service/impl/TraceDemoDataServiceImpl.java`
  - 权限 seed / 文档
- 实施步骤：
  1. 将 AdminController 限定为 dev/test profile，或拆分高危接口。
  2. 增加专用权限，如 `admin:dangerous` 或 `trace:data:clear`。
  3. `clearTraceData` 增加二次确认参数。
  4. 增加审计日志。
  5. `generateSampleData(count)` 增加最大值。
- 验收标准：
  - 非 dev/test 环境默认不能访问清空数据接口，或必须具备专用高危权限与确认参数。
  - count 超出上限返回业务错误。
  - 后端测试通过。
- 建议验证：
  - `mvn test -q`
  - Controller 权限/参数测试
- 验收结果：DONE（2026-05-02）
  - 新增 `TraceDemoDataProperties`，将示例数据管理开关与数量上限外置为 `trace.demo-data.*`；`application.yml` 默认 `enabled=false`、`max-generate-count=500`，`application-dev.yml` / `application-test.yml` 默认启用，`application-prod.yml` 显式保持关闭。
  - `AdminController` 已不再复用 `role:manage`：`generate-sample-data` 改为 `@RequirePermission("trace:data:generate")`，`clear-trace-data` 改为 `@RequirePermission("trace:data:clear")`，且清理接口必须携带 `confirm=DELETE_TRACE_DATA` 二次确认参数，否则返回 `BizCode.PARAM_ERROR`。
  - `TraceDemoDataServiceImpl` 已对两类示例数据管理操作增加环境开关校验、审计日志和 `count` 上限校验：接口关闭时返回 `403/BizCode.FORBIDDEN`，`count` 非 `1~500` 时返回 `400/BizCode.PARAM_ERROR`。
  - `init_schema.sql` 已补充 `trace:data:generate` / `trace:data:clear` 权限 seed，并新增 `backend/sql/migrate_v4_admin_demo_permissions.sql` 以便现有库幂等补齐权限与角色关联。
  - `README.md` 与 `api-doc.md` 已同步说明 dev/test 默认启用边界、专用权限要求、`count<=500` 约束及 `confirm=DELETE_TRACE_DATA` 调用方式。
  - 验证：`mvn test -Dtest=AdminControllerTest,TraceDemoDataServiceImplTest -q` 通过（8 tests）；`mvn test -q` 通过。
- 是否涉及前端 UI 文件：否

### R-P1-01 自助注册链路与后台用户创建解耦

- 优先级：P1
- 状态：DONE
- 来源审查项：登录页注册调用 `/api/users`，且硬编码 `roleId/status`
- 目标：自助注册只走 `/api/auth/register`；后台用户创建仍走 `/api/users`
- 重点范围：
  - `frontend/src/shared/components/Login.vue`
  - `frontend/src/core/api/auth.js`
  - `frontend/src/features/user/api/users.js`
  - `backend/src/main/java/com/example/trace/controller/AuthController.java`
  - `README.md` / `api-doc.md` / `postman-guide.md`
- 实施步骤：
  1. 在 `auth.js` 增加 `register(username, password)`。
  2. `Login.vue` 注册逻辑改调用 `/auth/register`。
  3. 删除前端注册中的 `roleId: 2` 和 `status: 1`。
  4. 确认注册成功后产品行为：自动登录或提示重新登录。
  5. 更新测试和文档。
- 验收标准：
  - 未登录注册请求为 `POST /api/auth/register`。
  - `/api/users` 仍需登录与 `user:manage` 权限。
  - 登录页注册测试通过。
- 建议验证：
  - `npm run test:run`
  - `mvn test -q`
- 验收结果：DONE（2026-05-02）
  - `frontend/src/core/api/auth.js` 已新增 `register(username, password)`，未登录注册请求统一走 `POST /api/auth/register`，不再复用用户管理接口。
  - `frontend/src/shared/components/Login.vue` 已将注册逻辑从 `createUser(...)` 改为 `registerUser(...)`，并删除原先硬编码的 `roleId: 2` / `status: 1`；注册成功后的产品行为保持为“提示注册成功，请登录”。
  - `frontend/src/features/user/api/users.js` 保持后台管理入口语义不变：`createUser(...)` 仍仅对应 `/api/users`，继续服务于已登录且具备 `user:manage` 权限的后台用户创建场景。
  - 已补充前端聚焦验证：`api-contracts.test.js` 断言 `/auth/register` 与 `/users` 的边界分离；`Login.test.js` 覆盖登录页注册切换与提交行为，确保不会再携带管理专用字段。
  - 已补充后端聚焦验证：`AuthControllerTest` 新增自助注册成功用例，确认注册链路继续落到默认 `USER` 角色与启用状态，并返回 201 Created。
  - 验证：`npm run test -- --run src/shared/components/__tests__/Login.test.js src/features/__tests__/api-contracts.test.js` 通过（8 tests）；`mvn test -Dtest=AuthControllerTest -q` 通过；`mvn test -q` 通过。
- 是否涉及前端 UI 文件：是，但属于功能/接口修复，不做视觉改造

### R-P1-02 提取 AuthService，降低 AuthController 厚度

- 优先级：P1
- 状态：DONE
- 来源审查项：`AuthController` 直接依赖 Mapper 并承载登录、注册、刷新、改密业务
- 目标：认证业务进入 service 层，Controller 只保留 HTTP 入口
- 重点范围：
  - `backend/src/main/java/com/example/trace/controller/AuthController.java`
  - 新增 `backend/src/main/java/com/example/trace/service/AuthService.java`
  - 新增 `backend/src/main/java/com/example/trace/service/impl/AuthServiceImpl.java`
  - AuthController 相关测试
- 实施步骤：
  1. 提取 login/register/refresh/changePassword/getCurrentUser 方法。
  2. Mapper 依赖移动到 AuthServiceImpl。
  3. Controller 保持 API 路径和响应兼容。
  4. 更新单元测试。
- 验收标准：
  - AuthController 不再直接操作 SysUserMapper/SysRoleMapper。
  - 登录、注册、刷新、改密行为与原接口兼容。
  - 后端测试通过。
- 建议验证：
  - `mvn test -q`
- 验收结果：DONE（2026-05-02）
  - 已新增 `backend/src/main/java/com/example/trace/service/AuthService.java` 与 `backend/src/main/java/com/example/trace/service/impl/AuthServiceImpl.java`，将 `login/register/logout/refresh/getCurrentUser/changePassword` 的认证业务统一下沉到服务层。
  - `AuthController` 已移除对 `SysUserMapper`、`SysRoleMapper`、`JwtUtil`、`PasswordEncoder`、`TokenStore`、`PermissionService` 的直接依赖，当前仅负责 HTTP 绑定、请求头/请求属性提取与响应包装。
  - 认证逻辑兼容性保持不变：注册仍返回 `201 Created`，刷新 Token 仍支持 `remember_me` 查询参数，改密后仍会递增 `tokenVersion` 并拉黑当前 Token。
  - 已将原先依附在控制器测试中的业务断言迁移到 `AuthServiceImplTest`；`AuthControllerTest` 现聚焦委派行为，`ControllerQueryBindingTest` 继续验证 `/api/auth/refresh?remember_me=true` 等 HTTP 绑定契约。
  - 验证：`mvn test -Dtest=AuthControllerTest,AuthServiceImplTest,ControllerQueryBindingTest -q` 通过（24 tests）；`mvn test -q` 通过。
- 是否涉及前端 UI 文件：否

### R-P1-03 扫码权限模型收敛

- 优先级：P1
- 状态：DONE
- 来源审查项：`trace:scan` 与 `trace:inbound/outbound/transfer` 的注释、实现、前端入口不一致
- 目标：统一权限语义，避免用户看到入口但后端拒绝，或配置人员误解权限
- 重点范围：
  - `backend/src/main/java/com/example/trace/controller/TraceController.java`
  - `backend/src/main/java/com/example/trace/security/permission/PermissionInheritanceResolver.java`
  - 权限初始化 SQL / 迁移脚本
  - `frontend/src/core/router/index.js`
  - `frontend/src/shared/components/layout/layoutNavigation.js`
  - 权限文档
- 实施步骤：
  1. 确认 `trace:scan` 的最终语义。
  2. 调整 Controller 细粒度校验。
  3. 调整权限继承规则。
  4. 调整前端路由与按钮权限判断。
  5. 更新 SQL seed、README、api-doc。
  6. 增加权限矩阵测试。
- 验收标准：
  - 权限注释、实现、SQL、前端入口、文档一致。
  - `trace:scan` 和专用权限组合的行为有测试覆盖。
- 建议验证：
  - `mvn test -q`
  - `npm run test:run`
- 验收结果：DONE（2026-05-02）
  - 已基于现有迁移脚本与接口文档将 `trace:scan` 明确收敛为**超级扫码权限**：可执行所有扫码动作；`trace:inbound` / `trace:outbound` / `trace:transfer` 为细粒度权限，仅允许对应动作。
  - `PermissionInheritanceResolver` 已调整为：`trace:create` / `trace:scan` / `trace:inbound` / `trace:outbound` / `trace:transfer` 均自动包含 `trace:view`，但细粒度扫码权限不再自动升级为 `trace:scan`。
  - `TraceController` 已改为 `@RequirePermission({"trace:scan","trace:inbound","trace:outbound","trace:transfer"})` 的统一入口，并新增动作级校验：`trace:scan` 可执行所有扫码动作，细粒度权限仅允许对应动作，`EXCEPTION/CORRECTION` 等通用动作仅允许 `trace:scan`。
  - 前端入口已同步收敛：`/scan` 路由与主导航不再因单独 `trace:view` 暴露；`ScanHub.vue` 中入库/出库/流转按钮改为“`trace:scan` 或对应细粒度权限”判定，避免超级扫码用户看不到入口，也避免细粒度角色误看到越权动作。
  - SQL 与文档已同步：`init_schema.sql` 直接补齐 `trace:inbound/outbound/transfer` seed 与 WAREHOUSE/LOGISTICS 角色分配；`migrate_v3_fine_grained_permissions.sql` 注释更新；`README.md`、`api-doc.md`、`postman-guide.md` 已改为新的权限语义与矩阵说明。
  - 已补充测试覆盖：后端新增 `TraceControllerTest` 验证超级扫码与细粒度权限矩阵，更新 `PermissionServiceTest` / `PermissionInheritanceResolverTest`；前端新增扫描中心路由/导航权限测试与 `ScanHub` 按钮可见性测试。
  - 验证：`npm run test -- --run src/shared/components/layout/__tests__/layoutNavigation.permissions.test.js src/features/trace/views/__tests__/ScanHub.permissions.test.js` 通过（5 tests）；`npm run build` 通过（仅既有 chunk size warning）；`mvn test -Dtest=TraceControllerTest,PermissionServiceTest,PermissionInheritanceResolverTest -q` 通过（13 tests）；`mvn test -q` 通过。
- 是否涉及前端 UI 文件：可能涉及路由/导航逻辑，但不做视觉改造

### R-P1-04 事件 remark 契约收敛

- 优先级：P1
- 状态：DONE
- 来源审查项：前端发送 `remark`，后端 DTO/entity/schema 没有该字段
- 目标：决定 remark 去留，并使前端、后端、DB、验链、文档一致
- 重点范围：
  - `frontend/src/features/trace/components/ScanFlowDialog.vue`
  - `backend/src/main/java/com/example/trace/dto/ScanTraceRequest.java`
  - `backend/src/main/java/com/example/trace/entity/TraceLifecycleLog.java`
  - `backend/sql/init_schema.sql`
  - `TraceLogFactory` / `HashUtil` / `SignatureUtil` 相关签名数据
  - `api-doc.md` / `postman-guide.md`
- 实施步骤：
  1. 确认 remark 是否是正式审计字段。
  2. 如保留：新增 DB 字段、DTO、实体、mapper、响应、hash/signature 数据。
  3. 如不保留：删除前端字段和文档中的相关描述。
  4. 增加契约测试。
- 验收标准：
  - 前端发送的字段不会被静默丢弃。
  - 如 remark 保留，验链逻辑覆盖该字段。
  - 文档与代码一致。
- 建议验证：
  - `mvn test -q`
  - `npm run test:run`
- 验收结果：DONE（2026-05-02）：
  - 确认保留 `remark` 为正式可选审计字段；新增 DTO、实体、Mapper、初始化 schema 与 `migrate_v5_event_remark.sql`，避免前端备注被静默丢弃。
  - `TraceScanTransactionService` 在持久化前 trim/空值归一化并限制备注最长 255 字符，拒绝 `<`、`>` 与控制字符；`TraceTimeline` 在详情历史中展示非空备注。
  - `HashUtil`、`SignatureUtil.buildSignatureData(...)`、`TraceLogFactory`、`TraceChainVerifyService` 均覆盖非空 `remark`；空备注不追加到载荷，保持历史空备注日志验链兼容。
  - `api-doc.md`、`postman-guide.md`、`README.md`、`backend/README.md` 已同步 remark 请求/响应、落库与验链说明。
  - 验证：`mvn test -Dtest=ScanTraceRequestValidationTest,TraceScanTransactionServiceTest,TraceChainVerifyServiceTest -q` 通过；`mvn test -q` 通过；`npm run test -- --run src/features/trace/components/__tests__/ScanFlowDialog.contract.test.js` 通过（1 test）；`npm run build` 通过（仅既有 chunk size warning）；`npm run test:run` 仍失败于既有 R-P1-08 范围的 `useConfirm.test.js` / `MainLayout.test.js`（2 files，5 tests）。
- 是否涉及前端 UI 文件：可能涉及表单字段去留；这是契约修复，不做视觉改造

### R-P1-05 eventTime 严格校验，禁止解析失败 fallback now

- 优先级：P1
- 状态：DONE
- 来源审查项：`DateTimeUtil.parseOrNow()` 解析失败返回当前时间
- 目标：保证溯源事件业务时间准确，非法格式直接失败
- 重点范围：
  - `backend/src/main/java/com/example/trace/util/DateTimeUtil.java`
  - `backend/src/main/java/com/example/trace/dto/ScanTraceRequest.java`
  - `backend/src/main/java/com/example/trace/service/impl/support/TraceScanTransactionService.java`
  - `frontend/src/features/trace/components/ScanFlowDialog.vue`
  - API 文档
- 实施步骤：
  1. 统一时间格式，建议 ISO-8601 或明确 `yyyy-MM-dd HH:mm:ss`。
  2. 后端解析失败抛业务异常并返回 400。
  3. 前端按统一格式提交。
  4. 文档更新请求示例。
  5. 增加非法时间格式测试。
- 验收标准：
  - 非法 eventTime 不会写入当前时间。
  - 缺省 eventTime 的业务规则明确：允许为空则后端使用 now；不允许为空则 400。
  - 前后端测试通过。
- 建议验证：
  - `mvn test -q`
  - `npm run test:run`
- 是否涉及前端 UI 文件：可能涉及提交格式逻辑，不做视觉改造
- 验收结果：DONE（2026-05-02）
  - `eventTime` 规则明确为可选：省略/空值沿用服务器当前时间；非空值必须为 ISO-8601 本地时间（示例 `2026-01-16T10:30:00`）。
  - `DateTimeUtil.parseOrNow()` 已移除解析失败 fallback now；非法非空值抛出 `BizException(PARAM_ERROR)`，HTTP 400。
  - 扫码事务入口在快照查询/写库前解析事件时间，非法格式不会触发持久化。
  - 前端扫码弹窗提交格式改为 `YYYY-MM-DDTHH:mm:ss`；接口文档、Postman 与 README 示例已同步。
  - 验证：`mvn test -Dtest=DateTimeUtilTest,TraceScanTransactionServiceTest,ScanTraceRequestValidationTest -q` 通过；`mvn test -q` 通过；`npm run test -- --run src/features/trace/components/__tests__/ScanFlowDialog.contract.test.js` 通过（1 test）；`npm run build` 通过（仅既有 chunk size warning）。

### R-P1-06 配件删除引用完整性保护

- 优先级：P1
- 状态：DONE
- 来源审查项：`PartServiceImpl` 删除前 TODO 未实现；schema 无 SPU 外键
- 目标：防止删除已参与溯源的配件，避免孤儿数据
- 重点范围：
  - `backend/src/main/java/com/example/trace/service/impl/PartServiceImpl.java`
  - `backend/src/main/java/com/example/trace/mapper/TraceSnapshotMapper.java`
  - `backend/src/main/java/com/example/trace/mapper/TraceLifecycleLogMapper.java`
  - `backend/sql/init_schema.sql`
  - PartService 测试
- 实施步骤：
  1. 删除前检查 `trace_snapshot.spu_id` 和 `trace_lifecycle_log.spu_id` 引用。
  2. 批量删除时识别被引用 ID。
  3. 有引用时返回明确业务错误。
  4. 评估软删除或增加 DB 外键/索引。
- 验收标准：
  - 已被溯源引用的配件无法被删除。
  - 未引用配件可正常删除。
  - 批量删除行为清晰且有测试覆盖。
- 建议验证：
  - `mvn test -q`
- 是否涉及前端 UI 文件：否
- 验收结果：DONE（2026-05-03）
  - `PartServiceImpl.deletePart()` 删除前会检查 `trace_snapshot.spu_id` 与 `trace_lifecycle_log.spu_id` 引用；已参与溯源的配件返回 `BizCode.CONFLICT` / HTTP 409，不再继续删除。
  - `batchDelete()` 会先归一化 ID、聚合识别被引用 SPU；任一 ID 被引用时整批拒绝，避免部分删除造成接口语义不清。
  - `init_schema.sql` 新增 `spu_id` 索引与 `ON DELETE RESTRICT` 外键；新增 `migrate_v6_part_reference_constraints.sql` 供既有库升级。
  - API/Postman/README/backend README 已补充已引用配件删除返回 409 的说明。
  - 验证：`mvn test -Dtest=PartServiceImplTest,TraceDemoDataServiceImplTest -q` 通过；`mvn test -q` 通过。

### R-P1-07 赋码 quantity 与示例数据 count 增加上限

- 优先级：P1
- 状态：DONE
- 来源审查项：生产赋码和示例数据生成缺少数量上限
- 目标：避免长事务、资源耗尽和误生成大量数据
- 重点范围：
  - `backend/src/main/java/com/example/trace/dto/ProduceAssignRequest.java`
  - `backend/src/main/java/com/example/trace/service/impl/support/TraceCodeAssignmentService.java`
  - `backend/src/main/java/com/example/trace/controller/AdminController.java`
  - `backend/src/main/java/com/example/trace/service/impl/TraceDemoDataServiceImpl.java`
- 实施步骤：
  1. 给 `quantity` 增加 `@Max`。
  2. 给示例数据 `count` 增加上限。
  3. 超限返回业务错误。
  4. 文档说明最大批量。
- 验收标准：
  - 超限请求被拒绝。
  - 合法数量请求仍可成功。
  - 测试覆盖边界值。
- 建议验证：
  - `mvn test -q`
- 是否涉及前端 UI 文件：否
- 验收结果：DONE（2026-05-03）
  - `ProduceAssignRequest.quantity` 新增 `@Min/@Max` 边界，单次生产赋码限定在 `1 ~ 500`；服务层在解析配件和创建集合前再次校验，超限返回 `BizCode.PARAM_ERROR` / HTTP 400，避免绕过 Bean Validation 后进入长事务。
  - `TraceCodeAssignmentServiceTest` 覆盖 `quantity=501` 拒绝且不访问 Mapper/LogFactory，以及 `quantity=500` 边界可正常生成 500 条溯源码。
  - `TraceDemoDataProperties` 新增 `HARD_MAX_GENERATE_COUNT=500`，`TRACE_DEMO_DATA_MAX_GENERATE_COUNT` 只能下调环境上限，不能配置超过硬上限；示例数据服务继续在写库前拒绝超限 `count`。
  - README、backend README、api-doc、postman-guide 与 `.env.example` 已同步最大批量说明。
  - 验证：`mvn test -Dtest=TraceCodeAssignmentServiceTest,RequestDtoContractTest,TraceDemoDataServiceImplTest,TraceDemoDataPropertiesTest -q` 通过；`mvn test -q` 通过。

### R-P1-08 前端测试恢复全绿并复核循环依赖

- 优先级：P1
- 状态：DONE
- 来源审查项：`npm run test:run` 当前 3 个文件失败、5 个测试失败；原记录提示 request/router/store 循环依赖
- 目标：恢复前端测试可信度，降低模块初始化耦合
- 重点范围：
  - `frontend/src/shared/composables/useConfirm.js`
  - `frontend/src/shared/composables/__tests__/useConfirm.test.js`
  - `frontend/src/shared/components/layout/MainLayout.vue`
  - `frontend/src/shared/components/layout/__tests__/MainLayout.test.js`
  - `frontend/src/core/api/request.js`
  - `frontend/src/core/router/index.js`
  - `frontend/src/core/stores/user.js`
- 实施步骤：
  1. 按当前 `useConfirm` 实现重写测试。
  2. 重新确认 MainLayout 导航权限预期并修测试或实现。
  3. 检查 `request -> router -> store -> auth api -> request` 是否仍构成循环依赖。
  4. 如存在，改为注入式 401 handler 或事件派发。
  5. 运行全量前端测试。
- 验收标准：
  - `npm run test:run` 全绿。
  - 401 跳转逻辑仍正常。
  - 不再因模块循环导致测试 suite 崩溃。
- 建议验证：
  - `npm run test:run`
  - `npm run build`
- 是否涉及前端 UI 文件：可能涉及 layout 逻辑，但不做视觉改造
- 验收结果：DONE（2026-05-03）
  - `useConfirm.test.js` 已按当前本地确认弹窗 composable 重写，直接断言 `isVisible/options/accept/reject` 状态和 Promise 结果，不再依赖已移除的 PrimeVue `useConfirm().require` 调用形态。
  - `MainLayout.test.js` 已按当前浮动导航/移动抽屉结构更新，覆盖桌面端权限过滤、移动端打开抽屉后导航关闭、退出登录确认与 `/login` 重定向；`AppFloatingNav` / `AppMobileNavDrawer` 仅补充稳定 `data-test` / `data-nav-path` 测试锚点，不改变视觉行为。
  - `request.js` 移除对 `@/core/router` 的直接导入，新增 `setUnauthorizedHandler`；`router/index.js` 注册 401 重定向 handler，从而打断 `request -> router -> store -> auth api -> request` 循环依赖，同时保持非登录 401 清理本地会话并跳转登录页的行为。
  - `request.test.js` 新增非登录 401 场景，验证清理 token/user、Toast 提示和注入式 unauthorized handler 调用。
  - 验证：`npm run test -- --run src/shared/composables/__tests__/useConfirm.test.js src/shared/components/layout/__tests__/MainLayout.test.js src/core/api/__tests__/request.test.js` 通过（12 tests）；`npm run test:run` 通过（23 files，63 tests）；`npm run build` 通过（仅既有 chunk size warning）。

### R-P1-09 关键文档与接口实现同步

- 优先级：P1
- 状态：DONE
- 来源审查项：README、backend README、api-doc、postman-guide 与实现漂移
- 目标：修正文档中会误导开发/部署/调试的关键内容
- 重点范围：
  - `README.md`
  - `backend/README.md`
  - `api-doc.md`
  - `postman-guide.md`
  - `frontend/README.md` 可先留到 R-P2-07
- 实施步骤：
  1. 修正注册接口说明。
  2. 修正 `/api/users/me` 为 `/api/auth/me`。
  3. 修正密钥自动生成是否落盘、生产密钥配置方式。
  4. 补充 profile、权限迁移、Redis 依赖说明。
  5. 同步 Postman 示例。
- 验收标准：
  - 文档中关键接口与代码一致。
  - 快速启动说明不会引导使用默认密钥或错误接口。
- 建议验证：
  - 文档内搜索 `/api/users/me`、默认密钥、`/api/auth/register`
- 是否涉及前端 UI 文件：否
- 验收结果：DONE（2026-05-03）
  - `README.md` / `backend/README.md` 已同步 profile、JWT secret、RSA 签名密钥外置、keyId/keyVersion、Redis 黑名单 fail-closed、权限迁移与默认账号密码规则，避免快速启动说明继续引导默认密钥或错误配置。
  - `api-doc.md` 已同步自助注册 `POST /api/auth/register`、当前用户 `GET /api/auth/me`、刷新 Token 查询参数 `remember_me`、登录/注册/刷新响应中的 `permissions` 字段，以及旧 `/api/trace/*` 路径“不再暴露”的迁移说明。
  - `api-doc.md` 前端 Axios/API 示例已改为当前 `frontend/src/core/api/request.js` / `@/core/api/request` 结构，`baseURL` 使用 `/api`，401 跳转通过 `setUnauthorizedHandler` 注入，不再展示旧 `src/utils/request.js`、旧 store 路径或直接 router 依赖。
  - `postman-guide.md` 已同步默认账号密码规则、登录密码、刷新参数 `remember_me=false`、Authorization `Bearer {{token}}` 和包含 `permissions` 的认证响应示例。
  - `backend/sql/init_schema.sql` 的默认账号注释已同步为“用户名 + 123456”规则。
  - 验证：文档一致性检索中 `/api/users/me`、`TraceSystemDefaultSecretKey`、`POST /users`、`@/utils/request`、`src/utils/request.js`、`rememberMe=false`、旧 `baseURL: 'http://localhost:8080'` 示例均无命中；`/api/trace/*` 仅保留在旧接口路径状态表中作为旧路径迁移提示。

### R-P2-01 CORS 处理机制收敛

- 优先级：P2
- 状态：DONE
- 来源审查项：MVC CORS 与自定义 `CorsFilter` 双实现
- 目标：降低跨域配置漂移和排查复杂度
- 重点范围：
  - `backend/src/main/java/com/example/trace/config/WebMvcConfig.java`
  - `backend/src/main/java/com/example/trace/config/CorsFilter.java`
  - `backend/src/main/java/com/example/trace/config/CorsProperties.java`
  - `backend/src/main/java/com/example/trace/config/CorsOriginMatcher.java`
- 实施步骤：
  1. 决定保留 MVC CORS 或 Filter CORS 的单一机制。
  2. 保留的机制覆盖 OPTIONS 和认证异常响应。
  3. dev/prod allowed origins 分离。
  4. 更新 CORS 测试。
- 验收标准：
  - 同一请求不会由两套逻辑分别写 CORS Header。
  - 允许/拒绝 Origin 的测试通过。
- 建议验证：
  - `mvn test -q`
- 是否涉及前端 UI 文件：否
- 验收结果：DONE（2026-05-03）
  - 已决定保留高优先级 `CorsFilter` 作为项目唯一 CORS 响应头写入点，覆盖普通跨域请求、OPTIONS 预检请求，以及登录/权限拦截器拒绝响应。
  - `WebMvcConfig` 已移除 `addCorsMappings` / `CorsRegistry` / `CorsProperties` 依赖，仅负责注册登录与权限拦截器，避免同一请求被 MVC CORS 和 Filter CORS 两套逻辑分别写 Header。
  - `CorsFilter` 继续使用 `CorsProperties` + `CorsOriginMatcher` 的外置白名单配置，并补充 `Access-Control-Expose-Headers: Authorization`，保持原 MVC 暴露 Header 行为。
  - 新增 `CorsFilterTest` 覆盖允许 Origin 写入 CORS Header 并继续请求、通配 Origin 的 OPTIONS 预检短路、拒绝 Origin 不写 CORS Header。
  - `README.md` / `backend/README.md` 已同步为“WebMvcConfig 仅拦截器配置、CorsFilter 为唯一 CORS 写入点”。
  - 验证：`mvn test "-Dtest=CorsOriginMatcherTest,CorsFilterTest" -q` 通过；`mvn test -q` 通过；`rg -n "addCorsMappings|CorsRegistry" backend/src/main/java` 无命中。

### R-P2-02 角色层级策略集中化

- 优先级：P2
- 状态：DONE
- 来源审查项：UserService/RoleService 重复硬编码角色优先级和系统角色
- 目标：统一角色层级、系统角色、可管理性策略
- 重点范围：
  - `backend/src/main/java/com/example/trace/service/impl/UserServiceImpl.java`
  - `backend/src/main/java/com/example/trace/service/impl/RoleServiceImpl.java`
  - 可新增 `RolePolicy` / `RoleHierarchyService`
- 实施步骤：
  1. 提取角色优先级和系统角色判断。
  2. UserService/RoleService 统一调用策略组件。
  3. 增加角色策略单元测试。
- 验收标准：
  - 角色优先级不再在多个 service 重复定义。
  - 现有权限测试通过。
- 建议验证：
  - `mvn test -q`
- 是否涉及前端 UI 文件：否
- 验收结果：DONE（2026-05-03）
  - 新增 `RolePolicy` 作为统一角色治理组件，集中维护 `SUPER_ADMIN > ADMIN > PRODUCER/WAREHOUSE/LOGISTICS/USER > 自定义角色` 的优先级、系统角色集合、管理角色集合、受保护权限前缀和 `superadmin` 保护账号。
  - `UserServiceImpl` 已移除本地 `ROLE_PRIORITY` / `getRolePriority`，创建/修改/改角色/批量删除/重置密码/列表过滤均改用 `RolePolicy` 判定。
  - `RoleServiceImpl` 已移除重复的角色优先级、系统角色和受保护权限前缀硬编码，更新/删除/分配权限和受保护权限委派均改用 `RolePolicy`。
  - 新增 `RolePolicyTest` 锁定角色优先级、严格高优先级管理规则、系统/管理角色集合、凭证重置保护和受保护权限筛选；同步调整 `UserServiceImplTest` / `RoleServiceImplTest` 构造注入。
  - `README.md` / `backend/README.md` 已补充 `service/policy` 目录与角色策略组件说明。
  - 验证：`mvn test "-Dtest=RolePolicyTest,RoleServiceImplTest,UserServiceImplTest" -q` 通过；`mvn test -q` 通过；`getRolePriority` 无命中，`ROLE_PRIORITY` 与 `PROTECTED_PERMISSION_PREFIXES` 仅保留在 `RolePolicy`。

### R-P2-03 用户 status 更新校验统一

- 优先级：P2
- 状态：DONE
- 来源审查项：`updateUser` 直接写 status，`toggleUserStatus` 才校验 0/1
- 目标：所有用户状态更新入口统一校验
- 重点范围：
  - `backend/src/main/java/com/example/trace/dto/UserUpdateRequest.java`
  - `backend/src/main/java/com/example/trace/service/impl/UserServiceImpl.java`
- 实施步骤：
  1. DTO 增加 status 取值校验。
  2. 提取 `validateStatus`。
  3. 增加 PUT 更新非法 status 测试。
- 验收标准：
  - PUT `/api/users/{id}` 不能写入非 0/1 状态。
  - 后端测试通过。
- 建议验证：
  - `mvn test -q`
- 是否涉及前端 UI 文件：否
- 验收结果：DONE（2026-05-03）
  - `UserUpdateRequest.status` 增加 Bean Validation：`@Min(0)` / `@Max(1)`，PUT `/api/users/{id}` 请求体在控制器层即可拒绝非 0/1 状态。
  - `UserCreateRequest.status` 同步增加 0/1 约束，保持用户创建与更新入口的状态契约一致。
  - `UserServiceImpl` 提取 `validateStatus` / `validateRequiredStatus`，`createUser`、`updateUser`、`toggleUserStatus` 在写库前统一校验状态值；`toggleUserStatus` 对 null 或非 0/1 状态返回 `BAD_REQUEST`，不再先进入权限/写库流程。
  - `UserServiceImplTest` 新增 PUT 更新非法 status 与 toggle 非法 status 的服务层回归测试，断言在加载用户前即返回 `BizCode.BAD_REQUEST`。
  - `RequestDtoContractTest` 新增 `UserUpdateRequest` Bean Validation 回归测试，锁定非法 status 的字段级约束。
  - 验证：`mvn test "-Dtest=UserServiceImplTest,RequestDtoContractTest" -q` 通过；`mvn test -q` 通过。

### R-P2-04 localStorage Token 风险评估与 CSP/短 Token 方案

- 优先级：P2
- 状态：DONE
- 来源审查项：Token 存 localStorage，XSS 后果放大
- 目标：在修复 XSS 后评估并落地补偿性认证安全措施
- 重点范围：
  - `frontend/src/core/stores/user.js`
  - `frontend/src/core/api/request.js`
  - 后端 JWT 过期配置
  - 部署 CSP 配置
- 实施步骤：
  1. 确认是否继续使用 localStorage。
  2. 若继续使用，缩短 access token 有效期并加 CSP。
  3. 评估 httpOnly Cookie + CSRF 或内存 token + refresh token。
  4. 形成方案文档或小步实施。
- 验收标准：
  - 有明确安全决策记录。
  - 如实施改造，登录、刷新、401 跳转测试通过。
- 建议验证：
  - `npm run test:run`
  - `mvn test -q`
- 是否涉及前端 UI 文件：否
- 验收结果：DONE（2026-05-03）
  - 已形成 `docs/security/token-storage-and-csp.md` 决策记录：当前版本继续使用 `Authorization` Header + `localStorage`，但将 `httpOnly Cookie + CSRF` 明确列为后续独立大改造路径。
  - 新增 `frontend/src/core/auth/authStorage.js`，将 token/user 的 `localStorage` 读写集中到单一适配层；`user` 缓存损坏时会清理并返回空值，避免启动异常。
  - `frontend/src/core/stores/user.js` 与 `frontend/src/core/api/request.js` 已改用存储适配层，401 清理与登录/登出缓存逻辑保持原契约。
  - `backend/src/main/resources/application.yml` 与 `backend/.env.example` 将默认 Token 有效期收敛为普通登录 2 小时、记住登录 1 天；`JwtUtil` 注释同步。
  - `frontend/index.html` 新增 CSP meta 基线：默认同源、禁止 object/form 外发，脚本仅允许同源与高德地图脚本域，图片/连接按地图和本地运行需要开白；文档补充生产 HTTP Header 示例。
  - `frontend/.env.example` 的地图 Key 改为占位符；README、backend README、api-doc、postman-guide 已同步短 Token 与 localStorage/CSP 补偿说明。
  - 新增 `authStorage.test.js`、`JwtUtilTest`、`SecurityDefaultsConfigTest` 锁定存储适配、2h/1d 有效期和配置默认值。
  - 验证：`npm run test -- --run src/core/auth/__tests__/authStorage.test.js src/core/api/__tests__/request.test.js` 通过（2 files，11 tests）；`mvn test "-Dtest=JwtUtilTest,SecurityDefaultsConfigTest" -q` 通过；`npm run test:run` 通过（24 files，67 tests）；`mvn test -q` 通过；`npm run build` 通过（仅既有 chunk size warning）。

### R-P2-05 工作区运行产物与大文件清理策略

- 优先级：P2
- 状态：DONE
- 来源审查项：根目录存在 crash log、replay log、大 CSV、运行截图等产物
- 目标：清理工作区卫生，防止运行产物混入源码管理
- 重点范围：
  - 根目录 `hs_err_pid*.log` / `replay_pid*.log` / `WizTree_*.csv`
  - `frontend/runtime_*.png`
  - `backend/backend_runtime*.log`
  - `frontend/vite_runtime*.log`
  - `.gitignore`
- 实施步骤：
  1. 确认哪些运行产物可删除或归档。
  2. 更新 `.gitignore`。
  3. 将必要诊断文件移动到 `runtime_state/` 或 `docs/analysis/`。
  4. 不做未经确认的破坏性删除。
- 验收标准：
  - 新运行产物不会默认混入源码目录。
  - 大文件处理有记录。
- 建议验证：
  - 检查根目录文件清单
- 是否涉及前端 UI 文件：否
- 验收结果：DONE（2026-05-03）
  - 根目录 `.gitignore` 已补充 `runtime_state/`、JVM crash/replay、`WizTree_*.csv`、backend/frontend runtime log、frontend 烟测截图、`frontend/dist/` 和 `frontend/node_modules/` 忽略规则。
  - `frontend/.gitignore` 同步补充 `runtime_*.png`、`vite_runtime*.log`、`vite_t*.log`，前端目录独立使用时也不会默认混入运行产物。
  - 未删除诊断文件；已将 21 个运行产物（共 219.27 MiB）非删除式归档到 `runtime_state/repo-hygiene/2026-05-03/`，并生成 `archive-manifest.json`。
  - 已新增 `docs/analysis/2026-05-03-runtime-artifacts-inventory.md` 记录原路径、归档路径、大小、恢复方式和忽略策略。
  - 验证：原位置 `hs_err_pid*.log`、`replay_pid*.log`、`WizTree_*.csv`、`frontend/runtime_*.png`、`frontend/vite_runtime*.log`、`frontend/vite_t*.log`、`backend/backend_runtime*.log`、`backend/hs_err_pid*.log` 剩余数均为 0；根目录大于 10MiB 文件扫描无命中。

### R-P2-06 全项目文本编码与乱码治理（含前端注释/JSDoc）

- 优先级：P2
- 状态：DONE
- 来源审查项：前端 API 文件注释存在 replacement-character artifacts；用户要求重启任务并扩展为全项目无乱码治理
- 目标：项目内源码、文档、配置、Postman、文章文本与可扫描运行态文本统一为 UTF-8 且不含实际乱码；新增防回归扫描，后续新增代码也必须通过
- 重点范围：
  - `frontend/src/features/user/api/users.js`
  - `frontend/src/features/trace/api/trace.js`
  - `frontend/src/features/part/api/parts.js`
  - `backend/src/main/java/com/example/trace/dto/RoleResponse.java`
  - `backend/src/main/java/com/example/trace/mapper/SysRolePermissionMapper.java`
  - `backend/src/main/java/com/example/trace/service/impl/RoleServiceImpl.java`
  - `frontend/src/features/trace/components/CreateTraceDialog.vue`
  - `frontend/src/features/trace/components/ScanFlowDialog.vue`
  - `frontend/src/core/api/__tests__/request.test.js`
  - `api-doc.md`、`docs/analysis`、`docs/specs`、`docs/progress`
  - `postman/role-governance-*.json`
  - `article/*.txt`、`文章/*.txt`
  - `runtime_state` 中可扫描文本产物
  - `tools/check_text_encoding.py`、`.editorconfig`
- 实施步骤：
  1. 将 GB18030 历史文本转换为 UTF-8。
  2. 修复后端/前端源码中的乱码注释、错误消息与测试占位字符串。
  3. 修复文档、Postman 集合/环境、进度文件和运行态文本产物中的乱码。
  4. 新增可复跑的全项目文本编码检查脚本，并用 `.editorconfig` 固化 UTF-8 编辑约束。
  5. 运行扫描、前端测试/构建和后端 Maven 测试。
- 验收标准：
  - `python tools/check_text_encoding.py` 通过。
  - `python tools/check_text_encoding.py --include-runtime` 通过。
  - 前端测试和构建通过。
  - 后端 Maven 测试通过。
- 建议验证：
  - `python tools/check_text_encoding.py`
  - `python tools/check_text_encoding.py --include-runtime`
  - `npm run test:run`
  - `npm run build`
  - `mvn test -q`
- 是否涉及前端 UI 文件：是（仅修复 `.vue` 注释，不调整交互、布局或视觉）
- 验收结果：DONE（2026-05-03）
  - 已将 `frontend/src/features/user/api/users.js`、`frontend/src/features/trace/api/trace.js`、`frontend/src/features/part/api/parts.js` 的 JSDoc 乱码注释重写为可读英文描述，保留原有参数/返回类型约定和 API 调用逻辑。
  - 扫描时发现 `frontend/src/features/dashboard/api/dashboard.js` 也存在同类 replacement-character JSDoc 乱码，已作为强相关 API 注释一并修复。
  - 本轮重启后，新增 `tools/check_text_encoding.py` 与 `.editorconfig`；扫描范围覆盖项目文本文件，默认排除三方依赖、构建输出和运行态归档，也支持 `--include-runtime` 扩展检查运行态文本产物。
  - 已将 6 个 GB18030 历史 `.txt` 文件转换为 UTF-8；修复后端角色 DTO/Mapper 注释、角色服务错误消息、前端弹窗注释、请求测试占位字符串、API 文档归档段、T21/T25 进度文档、Postman 角色治理集合/环境和 runtime_state 文本产物。
  - 验证：`python tools/check_text_encoding.py` 通过（442 files）；`python tools/check_text_encoding.py --include-runtime` 通过（667 files）；`npm run test:run` 通过（24 files，67 tests）；`npm run build` 通过（仅既有 chunk size warning）；`mvn test -q` 通过。

### R-P2-07 重写 frontend README

- 优先级：P2
- 状态：DONE
- 来源审查项：`frontend/README.md` 仍是 Vue 3 + Vite 模板说明
- 目标：补齐真实前端启动、环境变量、测试、构建、代理、HTTPS 说明
- 重点范围：
  - `frontend/README.md`
  - 可参考 `frontend/package.json`
  - 可参考 `frontend/.env.example`
  - 可参考 `frontend/vite.config.js`
- 实施步骤：
  1. 替换模板内容。
  2. 补充 npm scripts。
  3. 补充 HTTPS 证书和 API proxy 配置。
  4. 补充测试和构建命令。
  5. 明确地图 Key 配置方式。
- 验收标准：
  - README 能指导新开发者启动前端。
  - 不包含真实第三方 Key。
- 建议验证：
  - 文档审阅
- 是否涉及前端 UI 文件：否
- 验收结果：DONE（2026-05-03）
  - `frontend/README.md` 已从 Vue 3 + Vite 模板说明重写为项目级前端指南，覆盖技术栈、目录结构、前置条件、环境变量、依赖安装、HTTPS 证书、开发服务器、API 代理、npm scripts、测试、构建预览、路由权限和常见问题。
  - README 明确 `VITE_AMAP_KEY` 只能写入本机环境文件，示例中仅保留 `<replace-with-amap-web-js-api-key>` 占位符，不包含真实第三方 Key。
  - 验证：`python tools/check_text_encoding.py` 通过（442 files）；`python tools/check_text_encoding.py --include-runtime` 通过（667 files）；`npm run test:run` 通过（24 files，67 tests）；`npm run build` 通过（仅既有 chunk size warning）；README 搜索未命中 `This template`、真实高德 Key 片段。

### R-P2-08 补充启动路径与异常场景集成测试

- 优先级：P2
- 状态：DONE
- 来源审查项：现有测试缺少真实启动配置、初始化 SQL、权限迁移、Redis 异常、自助注册、扫码契约等覆盖
- 目标：把本轮审查暴露出的系统级风险纳入自动化验证
- 重点范围：
  - `backend/src/test/java/...`
  - `frontend/src/**/__tests__`
  - 可能新增 smoke/integration 测试脚本
- 建议覆盖：
  1. prod 配置缺少 JWT secret 时 fail-fast。
  2. Redis 异常时 Token 黑名单行为。
  3. `/api/auth/register` 匿名注册契约。
  4. `/api/users` 未登录不可访问。
  5. 扫码权限矩阵。
  6. 非法 eventTime 返回 400。
  7. 删除已引用配件失败。
  8. 地图 XSS 字段安全渲染。
- 验收标准：
  - 新增测试稳定通过。
  - 能防止 P0/P1 问题回归。
- 建议验证：
  - `mvn test -q`
  - `npm run test:run`
- 是否涉及前端 UI 文件：仅测试层，除非配合 R-P0-05 / R-P1-08
- 验收结果：DONE（2026-05-03）
  - 新增 `backend/src/test/java/com/example/trace/config/WebMvcSecurityPathContractTest.java`，直接验证 `WebMvcConfig` 的登录/权限拦截器注册顺序、包含/排除路径和关键启动路径匹配关系：`/api/auth/register` 与 `/api/traces/public-key` 匿名放行，`/api/users` 和业务 API 受登录拦截保护，权限拦截器在登录拦截器之后执行且排除 `/api/auth/**`。
  - 新增 `docs/analysis/2026-05-03-rp208-startup-and-exception-coverage.md`，把 prod fail-fast、Redis fail-closed、自助注册、用户管理入口、扫码权限矩阵、非法 eventTime、已引用配件删除、地图 XSS、前端 401 注入式处理等风险映射到现有自动化测试。
  - 跨任务验收清单已同步：`/api/users` 未登录不可访问、前端 README 可指导真实开发、启动路径与异常场景测试补齐均已关闭。
  - 验证：`python tools/check_text_encoding.py` 通过（444 files）；`python tools/check_text_encoding.py --include-runtime` 通过（669 files）；`mvn test "-Dtest=WebMvcSecurityPathContractTest"` 通过；`mvn test "-Dtest=WebMvcSecurityPathContractTest,ProdProfileConfigGuardTest,TokenStoreTest,LoginInterceptorTest,ControllerQueryBindingTest,AuthControllerTest,TraceControllerTest,PartServiceImplTest,ScanTraceRequestValidationTest" -q` 通过；`mvn test -q` 通过；`npm run test:run` 通过（24 files，67 tests）。

---

## 八、跨任务验收清单

完成每个阶段后建议统一检查：

### P0 阶段验收

- [x] 工作区不再保留真实 RSA 私钥。
- [x] 生产环境缺少 JWT secret 时启动失败。
- [x] 日志不打印完整 Authorization Header。
- [x] Redis 异常不会导致黑名单默认放行。
- [x] 地图业务字段不会作为 HTML 执行。
- [x] 清空溯源数据接口不在普通运行环境裸露。
- [x] `mvn test -q` 通过。

### P1 阶段验收

- [x] 登录页注册走 `/api/auth/register`。
- [x] `/api/users` 未登录不可访问。
- [x] 扫码权限模型在代码、SQL、前端、文档中一致。
- [x] `remark` 去留明确，且不会被静默丢弃。
- [x] 非法 eventTime 返回 400。
- [x] 已引用配件无法删除。
- [x] 批量数量上限生效。
- [x] `npm run test:run` 通过。

### P2 阶段验收

- [x] CORS 处理机制单一且测试覆盖。
- [x] 角色层级策略集中化。
- [x] 用户 status 校验统一。
- [x] Token 存储策略有安全决策记录。
- [x] 运行产物清理策略明确。
- [x] 前端 README 可指导真实开发。
- [x] 启动路径与异常场景测试补齐。

---

## 九、更新记录

| 日期 | 任务编号 | 状态变化 | 修改摘要 | 验证结果 |
|---|---|---|---|---|
| 2026-05-02 | INIT | 新建 | 根据 `项目审查.md` 生成新的审查整改任务表 | 未执行代码验证 |
| 2026-05-02 | R-P0-01 | TODO → DOING → DONE | 拆分 backend dev/test/prod profile；新增生产配置 fail-fast 守卫；移除 JwtUtil 默认 JWT secret 与短密钥补齐；将 MyBatis stdout SQL 限定到 dev/test；`.env.example` 改为占位符；同步 T21 进度备注 | `mvn test -Dtest=ProdProfileConfigGuardTest,AuthControllerTest,LoginInterceptorTest,CorsOriginMatcherTest` 通过（13 tests）；`mvn test -q` 通过 |
| 2026-05-02 | R-P0-02 | TODO → DOING → DONE | 删除 `backend/keys/private_key.pem` / `public_key.pem`；dev/test 签名密钥改为内存自动生成；prod 签名密钥路径强制外部注入并新增 fail-fast；补充 SignatureUtil 与 prod guard 测试；同步 README、backend README、env 模板和 T21 进度备注 | `mvn test -Dtest=SignatureUtilTest,ProdProfileConfigGuardTest,TraceChainVerifyServiceTest,TraceServiceImplTest` 通过（18 tests）；`mvn test -q` 通过；`backend/keys/*.pem` 不存在 |
| 2026-05-02 | R-P0-02 | DONE → DOING → DONE | 按用户提供的 `D:/trace-runtime/keys` 备份密钥配置 dev/test 固定外部 key；新增签名 keyId/keyVersion 配置、落库字段、迁移 SQL、Mapper/DTO/API 返回与 key-aware 验签；修正文档说明历史签名不会因重启失效 | `mvn test -Dtest=SignatureUtilTest,ProdProfileConfigGuardTest,TraceChainVerifyServiceTest,TraceServiceImplTest,TraceScanTransactionServiceTest,TraceDemoDataServiceImplTest` 通过（24 tests）；`mvn test -q` 通过 |
| 2026-05-02 | R-P0-03 | DOING → DONE | 删除 `LoginInterceptor` 完整 Authorization Header 原文日志并补日志回归测试；`JwtUtil` / `TokenStore` 异常日志改为固定原因或异常类型；`AuthController` / `UserServiceImpl` 登录失败、改密/重置密码日志改为 credential/session 语义；复核 prod/default 不启用 MyBatis stdout SQL | `mvn test -Dtest=LoginInterceptorTest,AuthControllerTest,ProdProfileConfigGuardTest` 通过（16 tests）；`mvn test -q` 通过；Authorization 原文日志模式与 prod/default `StdOutImpl` 扫描均无命中 |
| 2026-05-02 | R-P0-04 | TODO → DOING → DONE | 新增 `TokenStoreException`；`TokenStore.isBlacklisted()` Redis 查询异常不再返回 false；登录拦截器在黑名单不可用时 fail-closed 返回 503；登出/刷新/改密黑名单写入失败向上暴露；全局异常处理器统一返回认证状态存储不可用 | `mvn test -Dtest=TokenStoreTest,LoginInterceptorTest,AuthControllerTest,ControllerQueryBindingTest` 通过（21 tests）；`mvn test -q` 通过 |
| 2026-05-02 | R-P0-05 | TODO → DOING → DONE | 地图 InfoWindow 改用 DOM API + `textContent` 渲染业务字段并补 XSS 回归测试；新增扫码节点/省市字段长度与安全字符校验；事务入口在持久化前二次 normalize/校验非法字段 | `npm run test -- --run src/features/trace/components/__tests__/TraceRouteMap.info-window.test.js` 通过（2 tests）；`npm run build` 通过；`mvn test -Dtest=ScanTraceRequestValidationTest,TraceScanTransactionServiceTest` 通过（6 tests）；`mvn test -q` 通过 |
| 2026-05-02 | R-P0-06 | TODO → DOING → DONE | 新增 `TraceDemoDataProperties` 统一控制示例数据管理开关与数量上限；Admin 接口改用 `trace:data:generate` / `trace:data:clear` 专用权限；清空接口增加 `confirm=DELETE_TRACE_DATA` 二次确认；服务层增加环境禁用与 `count<=500` 校验、审计日志；补充权限 seed/migration 与 README/api-doc 说明 | `mvn test -Dtest=AdminControllerTest,TraceDemoDataServiceImplTest -q` 通过（8 tests）；`mvn test -q` 通过 |
| 2026-05-02 | R-P1-01 | TODO → DOING → DONE | 登录页注册从 `/api/users` 切换到 `/api/auth/register`；新增 `auth.register()` 并移除前端硬编码 `roleId/status`；保持 `/api/users` 仅供后台管理创建用户；补充登录页注册与 API 边界测试，并新增后端默认角色注册用例 | `npm run test -- --run src/shared/components/__tests__/Login.test.js src/features/__tests__/api-contracts.test.js` 通过（8 tests）；`mvn test -Dtest=AuthControllerTest -q` 通过；`mvn test -q` 通过 |
| 2026-05-02 | R-P1-02 | TODO → DOING → DONE | 新增 `AuthService` / `AuthServiceImpl` 并将登录、注册、登出、刷新、当前用户、改密业务从 `AuthController` 下沉到服务层；控制器仅保留 HTTP 入口与响应包装；新增服务层聚焦测试并保留控制器委派/绑定测试 | `mvn test -Dtest=AuthControllerTest,AuthServiceImplTest,ControllerQueryBindingTest -q` 通过（24 tests）；`mvn test -q` 通过 |
| 2026-05-02 | R-P1-03 | TODO → DOING → DONE | 明确 `trace:scan` 为超级扫码权限；细粒度 `trace:inbound/outbound/transfer` 仅允许对应动作且只继承 `trace:view`；收敛 `TraceController` 动作校验、扫描中心路由/导航与 `ScanHub` 按钮权限判断；同步 seed SQL 与权限文档，并补充前后端权限矩阵测试 | `npm run test -- --run src/shared/components/layout/__tests__/layoutNavigation.permissions.test.js src/features/trace/views/__tests__/ScanHub.permissions.test.js` 通过（5 tests）；`npm run build` 通过；`mvn test -Dtest=TraceControllerTest,PermissionServiceTest,PermissionInheritanceResolverTest -q` 通过（13 tests）；`mvn test -q` 通过 |
| 2026-05-02 | R-P1-04 | TODO → DOING → DONE | 保留事件 `remark` 为正式可选审计字段；新增后端 DTO/entity/mapper/schema/migration 与备注校验/归一化；非空备注纳入 Hash 与 RSA 签名载荷并由验链覆盖；详情时间线展示非空备注；同步 README、backend README、api-doc、postman-guide 与进度备注 | `mvn test -Dtest=ScanTraceRequestValidationTest,TraceScanTransactionServiceTest,TraceChainVerifyServiceTest -q` 通过；`mvn test -q` 通过；`npm run test -- --run src/features/trace/components/__tests__/ScanFlowDialog.contract.test.js` 通过（1 test）；`npm run build` 通过（仅既有 chunk size warning）；`npm run test:run` 仍有既有 R-P1-08 红灯：`useConfirm.test.js` / `MainLayout.test.js`（2 files，5 tests） |
| 2026-05-02 | R-P1-05 | TODO → DOING → DONE | `DateTimeUtil` 改为严格 ISO-8601 本地时间解析，保留空值使用 now 的历史规则；非法非空 eventTime 抛 `PARAM_ERROR`/400 且在事务写库前失败；前端扫码弹窗改交 `YYYY-MM-DDTHH:mm:ss`；README、backend README、api-doc、postman-guide 与进度备注同步 | `mvn test -Dtest=DateTimeUtilTest,TraceScanTransactionServiceTest,ScanTraceRequestValidationTest -q` 通过；`mvn test -q` 通过；`npm run test -- --run src/features/trace/components/__tests__/ScanFlowDialog.contract.test.js` 通过（1 test）；`npm run build` 通过（仅既有 chunk size warning） |
| 2026-05-03 | R-P1-06 | TODO → DOING → DONE | `PartServiceImpl` 删除/批量删除前聚合检查 `trace_snapshot` 与 `trace_lifecycle_log` 的 SPU 引用；已参与溯源的配件返回 `CONFLICT`/409 且批量删除整批拒绝；补充 mapper 查询、服务层回归测试、`init_schema.sql` 外键/索引和 `migrate_v6_part_reference_constraints.sql`；同步 API/Postman/README 说明 | `mvn test -Dtest=PartServiceImplTest,TraceDemoDataServiceImplTest -q` 通过；`mvn test -q` 通过 |
| 2026-05-03 | R-P1-07 | TODO → DOING → DONE | `ProduceAssignRequest.quantity` 增加 `@Max(500)` 并由 `TraceCodeAssignmentService` 在数据库访问前二次校验；示例数据 `TRACE_DEMO_DATA_MAX_GENERATE_COUNT` 增加 500 硬上限且只能下调；补充 quantity 超限/500 边界、DTO Bean Validation、demo count 配置硬上限测试；同步 README、backend README、api-doc、postman-guide 和 `.env.example` 批量上限说明 | `mvn test -Dtest=TraceCodeAssignmentServiceTest,RequestDtoContractTest,TraceDemoDataServiceImplTest,TraceDemoDataPropertiesTest -q` 通过；`mvn test -q` 通过 |
| 2026-05-03 | R-P1-08 | TODO → DOING → DONE | 重写 `useConfirm` 当前实现测试；按浮动导航/移动抽屉现状修复 `MainLayout` 测试并补稳定测试锚点；`request.js` 移除直接 router 依赖，改由 `setUnauthorizedHandler` 注入 401 跳转逻辑，打断 request/router/store/auth api 循环；补充 401 handler 合同测试 | `npm run test -- --run src/shared/composables/__tests__/useConfirm.test.js src/shared/components/layout/__tests__/MainLayout.test.js src/core/api/__tests__/request.test.js` 通过（12 tests）；`npm run test:run` 通过（23 files，63 tests）；`npm run build` 通过（仅既有 chunk size warning） |
| 2026-05-03 | R-P1-09 | DOING → DONE | 同步 README、backend README、api-doc、postman-guide 与 init_schema 注释中的关键接口/配置说明：注册改为 `/api/auth/register`，当前用户改为 `/api/auth/me`，refresh 查询参数使用 `remember_me`，认证响应包含 `permissions`，旧 `/api/trace/*` 明确不再暴露；补齐 profile/JWT/RSA key metadata/Redis fail-closed/迁移脚本说明，并修正前端 Axios 示例为当前 `/api` baseURL 与注入式 401 handler | 文档一致性检索通过：`/api/users/me`、`TraceSystemDefaultSecretKey`、`POST /users`、`@/utils/request`、`src/utils/request.js`、`rememberMe=false`、旧 `baseURL: 'http://localhost:8080'` 示例均无命中；`/api/trace/*` 仅作为旧路径迁移提示保留 |
| 2026-05-03 | R-P2-01 | TODO → DOING → DONE | 保留 `CorsFilter` 为唯一 CORS 响应头写入点，移除 `WebMvcConfig.addCorsMappings` / `CorsRegistry` / `CorsProperties` 依赖；`WebMvcConfig` 仅保留登录/权限拦截器；`CorsFilter` 补充 `Access-Control-Expose-Headers: Authorization` 并新增允许/拒绝 Origin 与 OPTIONS 预检短路测试；同步 README/backend README CORS 说明 | `mvn test "-Dtest=CorsOriginMatcherTest,CorsFilterTest" -q` 通过；`mvn test -q` 通过；后端代码检索 `addCorsMappings` / `CorsRegistry` 无命中 |
| 2026-05-03 | R-P2-02 | TODO → DOING → DONE | 新增 `RolePolicy` 集中维护角色优先级、系统角色、管理角色集合、受保护权限前缀与 `superadmin` 保护账号；`UserServiceImpl` / `RoleServiceImpl` 移除重复 `ROLE_PRIORITY` / `getRolePriority` / 系统角色硬编码并统一调用策略组件；补充 `RolePolicyTest` 并调整用户/角色服务测试；同步 README/backend README 目录说明 | `mvn test "-Dtest=RolePolicyTest,RoleServiceImplTest,UserServiceImplTest" -q` 通过；`mvn test -q` 通过；`getRolePriority` 无命中，`ROLE_PRIORITY` 与 `PROTECTED_PERMISSION_PREFIXES` 仅保留在 `RolePolicy` |
| 2026-05-03 | R-P2-03 | TODO → DOING → DONE | `UserUpdateRequest.status` 和 `UserCreateRequest.status` 增加 `@Min(0)` / `@Max(1)`；`UserServiceImpl` 提取 `validateStatus` / `validateRequiredStatus` 并在 create/update/toggle 写库前统一校验；补充 PUT 更新非法 status、toggle 非法 status 与 DTO Bean Validation 回归测试 | `mvn test "-Dtest=UserServiceImplTest,RequestDtoContractTest" -q` 通过；`mvn test -q` 通过 |
| 2026-05-03 | R-P2-04 | TODO → DOING → DONE | 形成 Token 存储安全决策记录；保留现有 `Authorization` Header + `localStorage` 模型但新增 `authStorage` 单一适配层；JWT 默认有效期收敛为 2h / 记住登录 1d；前端新增 CSP meta 基线与生产 Header 建议；`.env.example` 地图 Key 改占位符；同步 README/backend README/API/Postman 与进度文档 | `npm run test -- --run src/core/auth/__tests__/authStorage.test.js src/core/api/__tests__/request.test.js` 通过（2 files，11 tests）；`mvn test "-Dtest=JwtUtilTest,SecurityDefaultsConfigTest" -q` 通过；`npm run test:run` 通过（24 files，67 tests）；`mvn test -q` 通过；`npm run build` 通过（仅既有 chunk size warning） |
| 2026-05-03 | R-P2-05 | TODO → DOING → DONE | 补充根目录与 frontend `.gitignore` 运行产物忽略规则；将 21 个 crash/replay log、WizTree CSV、runtime 截图和 runtime log 非删除归档到 `runtime_state/repo-hygiene/2026-05-03/`；新增 `docs/analysis/2026-05-03-runtime-artifacts-inventory.md` 记录归档清单与恢复方式 | 原位置相关产物剩余数均为 0；`runtime_state/repo-hygiene/2026-05-03/archive-manifest.json` 记录 21 个文件（219.27 MiB）；根目录 >10MiB 文件扫描无命中 |
| 2026-05-03 | R-P2-06 | TODO → DOING → DONE | 修复 `frontend/src/features/user/api/users.js`、`frontend/src/features/trace/api/trace.js`、`frontend/src/features/part/api/parts.js` 中的 JSDoc 乱码注释；扫描发现并一并修复强相关的 `frontend/src/features/dashboard/api/dashboard.js`；4 个文件统一重写为 UTF-8，仅调整注释/JSDoc，API 调用逻辑不变 | `frontend/src` 范围 replacement-character 扫描无命中；4 个 features API 文件 UTF-8 解码通过且无常见乱码码点；`npm run test:run` 通过（24 files，67 tests）；`npm run build` 通过（仅既有 chunk size warning） |
| 2026-05-03 | R-P2-06 | DONE → DOING → DONE（用户要求重启） | 将 R-P2-06 范围从前端 JSDoc 扩展为全项目文本编码治理；新增 `tools/check_text_encoding.py` 与 `.editorconfig`；转换 6 个 GB18030 历史文本；修复后端角色注释/错误消息、前端弹窗注释与测试占位、API/T21/T25 文档、Postman 角色治理集合/环境、runtime_state 文本产物中的乱码 | `python tools/check_text_encoding.py` 通过（442 files）；`python tools/check_text_encoding.py --include-runtime` 通过（667 files）；`npm run test:run` 通过（24 files，67 tests）；`npm run build` 通过（仅既有 chunk size warning）；`mvn test -q` 通过 |
| 2026-05-03 | R-P2-07 | TODO → DOING → DONE | 将 `frontend/README.md` 从 Vite 模板替换为真实前端指南；补齐环境变量、HTTPS 证书、API proxy、npm scripts、测试、构建、地图 Key 配置、路由权限与常见问题说明 | `python tools/check_text_encoding.py` 通过（442 files）；`python tools/check_text_encoding.py --include-runtime` 通过（667 files）；`npm run test:run` 通过（24 files，67 tests）；`npm run build` 通过（仅既有 chunk size warning）；README 未包含真实高德 Key 或 Vite 模板正文 |
| 2026-05-03 | R-P2-08 | TODO → DOING → DONE | 新增 `WebMvcSecurityPathContractTest` 锁定 `WebMvcConfig` 登录/权限拦截器路径契约与顺序；新增 R-P2-08 覆盖清单文档，将 prod fail-fast、Redis fail-closed、自助注册、用户管理入口、扫码权限矩阵、非法 eventTime、已引用配件删除、地图 XSS 等风险映射到自动化测试；同步跨任务验收清单 | `python tools/check_text_encoding.py` 通过（444 files）；`python tools/check_text_encoding.py --include-runtime` 通过（669 files）；`mvn test "-Dtest=WebMvcSecurityPathContractTest"` 通过；R-P2-08 相关后端定向测试通过；`mvn test -q` 通过；`npm run test:run` 通过（24 files，67 tests） |
