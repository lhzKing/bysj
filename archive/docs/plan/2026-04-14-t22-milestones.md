# T22 Milestones

## M1 - Scope Locked

- Consumers audited
- Frozen-file boundary confirmed
- Compatibility entry requirement confirmed

## M2 - Data Responsibility Split

- `REGIONS` moved to `catalog.js`
- `NODE_REGION_MAP` moved to `node-region-map.js`
- `getRegionByNode(...)` moved to `lookup.js`
- `regions.js` reduced to compatibility barrel

## M3 - Verification Closed

- Shared-data tests passed
- Existing scan-flow contract test passed
- Frontend build passed
- Docs and task table synchronized
